{
  "name": "basic-scala-lsp",
  "description": "LSP extension to provide support for basic Scala definitions",
  "author": "Matt Dziuban <mrdziuban@gmail.com>",
  "license": "UNLICENSED",
  "private": true,
  "version": "1.0.2",
  "repository": {
    "type": "git",
    "url": "https://github.com/mrdziuban/vscode-basic-scala"
  },
  "publisher": "mrdziuban",
  "engines": {
    "vscode": "^1.23.0"
  },
  "activationEvents": [
    "onLanguage:scala"
  ],
  "main": "./client/out/extension",
  "scripts": {
    "vscode:prepublish": "cd client && yarn update-vscode && cd .. && yarn compile",
    "compile:client": "tsc -p ./client/tsconfig.json",
    "compile:server": "tsc -p ./server/tsconfig.json",
    "watch:client": "tsc -w -p ./client/tsconfig.json",
    "watch:server": "tsc -w -p ./server/tsconfig.json",
    "compile": "yarn compile:client && yarn compile:server",
    "package": "vsce package --yarn",
    "postinstall": "cd client && yarn && cd ../server && yarn && cd .."
  },
  "devDependencies": {
    "@types/node": "~10.3.1",
    "typescript": "~2.9.1",
    "vsce": "~1.42.0"
  }
}
